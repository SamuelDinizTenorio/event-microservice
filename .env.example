# Exemplo de arquivo de configuração de ambiente.
# Copie este arquivo para .env e preencha com os valores para o seu ambiente de desenvolvimento.

# --- Configuração das Portas ---
# Porta que será exposta na sua máquina local para a aplicação.
APP_PORT=8080
# Porta que será exposta na sua máquina local para o banco de dados.
DB_PORT=5432

# --- Credenciais do Banco de Dados ---
# O host e o nome do banco são definidos no application.yaml (para dev local)
# e no docker-compose.yml (para ambiente containerizado).
DB_USER=postgres
DB_PASSWORD=sua_senha_aqui

# --- Configuração de Serviços Externos ---
# URL do microsserviço de e-mail.
EMAIL_SERVICE_URL=http://localhost:8081

# --- Regras de Negócio Configuráveis ---
# Duração mínima de um evento em minutos.
EVENT_MIN_DURATION_MINUTES=15
